export const featuredImages: string[] = [];
export const comparisonImages: string[] = [];

for (let i = 1; i < 8; ++i)
  featuredImages.push(`./images/featuredPortraits/${i}.jpg`);

for (let i = 1; i < 3; ++i)
  comparisonImages.push(`./images/comparison/${i}.jpg`);
